# Write a program to sort an ArrayList of custom Product objects (with name, price) by price using a Comparator interface. [High]

```java

import java.util.ArrayList;
import java.util.Collections;
import java.util.Comparator;

class Product {
    String name;
    double price;

    Product(String name, double price) {
        this.name = name;
        this.price = price;
    }

    @Override
    public String toString() {
        return "Product{name='" + name + "', price=" + price + "}";
    }
}

class PriceComparator implements Comparator<Product> {
    @Override
    public int compare(Product p1, Product p2) {
        // Ascending order
        return Double.compare(p1.price, p2.price);
    }
}

public class SortProducts {
    public static void main(String[] args) {
        // Create ArrayList of Product
        ArrayList<Product> products = new ArrayList<>();

        products.add(new Product("Laptop", 75000));
        products.add(new Product("Phone", 40000));
        products.add(new Product("Headphones", 2500));
        products.add(new Product("Monitor", 12000));

        System.out.println("Before Sorting:");
        for (Product p : products) {
            System.out.println(p);
        }

        // Sort using Comparator
        Collections.sort(products, new PriceComparator());

        System.out.println("\nAfter Sorting by Price (Ascending):");
        for (Product p : products) {
            System.out.println(p);
        }

        // If needed: sort in descending order using Comparator with lambda
        Collections.sort(products, (p1, p2) -> Double.compare(p2.price, p1.price));

        System.out.println("\nAfter Sorting by Price (Descending):");
        for (Product p : products) {
            System.out.println(p);
        }
    }
}


```

### Output:

```java

Before Sorting:
Product{name='Laptop', price=75000.0}
Product{name='Phone', price=40000.0}
Product{name='Headphones', price=2500.0}
Product{name='Monitor', price=12000.0}

After Sorting by Price (Ascending):
Product{name='Headphones', price=2500.0}
Product{name='Monitor', price=12000.0}
Product{name='Phone', price=40000.0}
Product{name='Laptop', price=75000.0}

After Sorting by Price (Descending):
Product{name='Laptop', price=75000.0}
Product{name='Phone', price=40000.0}
Product{name='Monitor', price=12000.0}
Product{name='Headphones', price=2500.0}


```
